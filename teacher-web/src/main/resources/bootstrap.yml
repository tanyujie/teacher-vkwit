server:
  port: 8081
spring:
  application:
    name: teacherweb
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: alan
        password: alan

encrypt:
  failOnError: false

